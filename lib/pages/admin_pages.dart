import 'package:flutter/material.dart';

class Admin_pages extends StatelessWidget {
  const Admin_pages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin'),
      ),
      body: Container(),
    );
  }
}
